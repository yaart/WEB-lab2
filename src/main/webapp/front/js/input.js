function getX() {
    const xInput = document.getElementById('xvalue');
    return xInput ? xInput.value : null;
}

function getY() {
    const yInput = document.getElementById('yvalue');
    return yInput ? yInput.value : null;
}


function getR() {
    const rInput = document.getElementById('rvalue');
    return rInput ? rInput.value : null;
}